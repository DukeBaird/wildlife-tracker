# wildlife-tracker

# Setup
- Install node 14
- In the root, run `npm install` to install all dependencies
- Configure `config.js` as necessary
- Run `npm run pack` to bundle
- Run `npm start`, and navigate to localhost:8080
